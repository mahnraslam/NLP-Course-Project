[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    llm: marks tests that require a real Gemini API key
    slow: marks tests that are slow (vision enrichment, large PDFs)